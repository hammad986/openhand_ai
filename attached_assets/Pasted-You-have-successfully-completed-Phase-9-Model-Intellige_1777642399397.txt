You have successfully completed Phase 9 (Model Intelligence Routing System).

Before proceeding further, perform two tasks in strict order:

---

# 🔴 STEP 0 — SYSTEM PREVIEW VALIDATION (MANDATORY)

The Preview is currently not rendering correctly.

You MUST:

1. Open the application in the browser (http://localhost:5000)
2. Run at least 2 tasks:

   * simple HTML generation
   * multi-step coding task
3. Verify:

   * Preview panel renders output correctly
   * iframe / live_url works
   * fallback to code viewer works if preview fails
4. Fix ANY issues related to:

   * blank preview
   * incorrect file path
   * artifact not loading
   * broken iframe source

Do NOT continue until preview is 100% working.

---

# 🚀 PHASE 10 — AGENT INTELLIGENCE + MEMORY SYSTEM

This phase upgrades the system from a router → intelligent agent.

---

# 🧠 STEP 1 — SHORT-TERM MEMORY (SESSION MEMORY)

Create:

* last 5 tasks memory buffer
* store:

  * task
  * result (success/fail)
  * model used
  * errors

Inject this memory into planning prompts.

Goal:
Avoid repeating mistakes.

---

# 🧠 STEP 2 — LONG-TERM MEMORY (PERSISTENT)

Use existing DB (or create new table):

Store:

* successful patterns
* failed attempts
* common fixes

Expose API:

GET /api/memory/recent
GET /api/memory/insights

---

# 🧠 STEP 3 — CONTEXT-AWARE PLANNING

Modify planning stage:

Before generating plan:

* include:

  * last task context
  * previous failures
  * similar task patterns

Prompt should include:

"Previously, this type of task failed due to X. Avoid that."

---

# 🧠 STEP 4 — SELF-CORRECTION LOOP (UPGRADE)

Enhance retry system:

Instead of simple retry:

* analyze previous error
* modify strategy
* retry with improved prompt

Limit:
max 2 retries

---

# 🧠 STEP 5 — AGENT INTELLIGENCE SCORE

Create scoring system:

* success rate
* retry count
* execution quality

Expose:

GET /api/agent/score

---

# 🎨 STEP 6 — UI INTEGRATION

Add to Inspector:

* "Memory Context"
* "Previous Failures"
* "Learned Insights"

Add to logs:

* show when memory is used

---

# ⚠️ RULES

* DO NOT break Phase 9 routing
* DO NOT slow down system
* Memory must be lightweight
* Avoid large context injection

---

# 📊 OUTPUT REQUIRED

1. Memory architecture
2. Updated planning logic
3. Retry improvement
4. UI integration
5. API endpoints

---

# 🚀 FINAL GOAL

System should behave like:

"An AI that learns from its past actions and improves automatically"

---

After completing Phase 10, confirm:

* preview working
* routing working
* memory working

Then we will proceed to Phase 11.
