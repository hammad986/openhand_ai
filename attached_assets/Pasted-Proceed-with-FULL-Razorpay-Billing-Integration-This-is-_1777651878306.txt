Proceed with FULL Razorpay Billing Integration.

This is the FINAL critical phase to convert the system into a real SaaS product.

---

# 🎯 OBJECTIVE

Implement a COMPLETE, PRODUCTION-READY billing system using Razorpay.

The current plan system (Lite / Pro / Elite) is UI-only — convert it into REAL paid subscriptions.

---

# 💳 PART 1 — RAZORPAY ORDER SYSTEM

Create endpoint:

POST /api/payments/create-order

Input:

* plan: "pro" or "elite"
* billing_cycle: "monthly" or "yearly"

Logic:

* Pro → ₹20/month
* Elite → ₹50/month

Return:

* razorpay_order_id
* amount
* currency

---

# 🔐 PART 2 — PAYMENT VERIFICATION

Create endpoint:

POST /api/payments/verify

Validate:

* razorpay_signature
* payment_id
* order_id

If valid:

* activate subscription
* store in DB

---

# 🔁 PART 3 — WEBHOOK SYSTEM (CRITICAL)

Create endpoint:

POST /api/payments/webhook

Handle:

* payment.captured
* payment.failed

IMPORTANT:

* Webhook must be source of truth
* Never trust frontend alone

---

# 🧾 PART 4 — SUBSCRIPTION DATABASE

Create table:

subscriptions:

* user_id
* plan
* status
* start_date
* expiry_date
* payment_id

Logic:

* auto-expire after time
* upgrade/downgrade support

---

# 📄 PART 5 — INVOICE SYSTEM

On payment success:

* generate invoice_id
* create HTML invoice

Fields:

* user email
* plan
* amount
* date

Add endpoint:

GET /api/invoice/<id>

Add button in UI:
"Download Invoice"

---

# 📧 PART 6 — EMAIL NOTIFICATIONS

Integrate:

Resend (preferred) or SendGrid

Send email on:

* payment success
* invoice
* plan activation

---

# 🎨 PART 7 — UI INTEGRATION

Update:

1. Upgrade modal:

* connect to Razorpay

2. Header badge:

* show plan + expiry

3. Inspector:

* show subscription status

4. Add:

* "Billing Info" section

---

# 🔐 PART 8 — SECURITY

* verify Razorpay signature
* webhook verification required
* prevent fake payments

---

# ⚙️ PART 9 — ENV VARIABLES

Add:

RAZORPAY_KEY_ID
RAZORPAY_KEY_SECRET
EMAIL_API_KEY

---

# 🧪 PART 10 — TESTING

Test:

* success payment
* failed payment
* webhook
* plan activation
* invoice generation

---

# 🚀 FINAL RESULT

System becomes:

✔ Real SaaS platform
✔ Payment-enabled
✔ Trustworthy
✔ Ready for users

---

After completion:

Run full validation and confirm:

"PAYMENT SYSTEM FULLY IMPLEMENTED AND VERIFIED"
